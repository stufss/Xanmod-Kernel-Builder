KERNEL_VER=6.18
# LLVM_VER=-$(ls /usr/bin/clang* | cut -f4 -d'/' | grep -E '^clang-[0-9]+$' | awk -F'-' '{ if ($2 > max) max = $2 } END { print max }')
LLVM_VER=-$(clang --version 2>/dev/null | head -n 1 | cut -f3 -d" " | cut -f1 -d".")
NVIDIA_VER=610.43.02
